[project]
name = "nemlig-cli"
version = "0.1.0"
description = "Minimal CLI for the nemlig.com grocery API, with JSON output for scripts/agents"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "argcomplete>=3.0.0",
]

[project.scripts]
nemlig = "nemlig_cli:main"
